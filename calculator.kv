#:kivy 1.10.1

<CustomButton@Button>:
    font_size: 32

<CalculatorLayout>:
    id: calculator
    display: entry
    rows: 6
    padding: 10
    spacing: 10

    BoxLayout:
        TextInput:
            id: entry
            font_size: 32
            multiline: False
            readonly: True
    
    BoxLayout:
        spacing: 10
        CustomButton:
            text: "AC"
            background_color: .937, .208, .063, 1
            on_press: entry.text = ""

        CustomButton:
            text: "="
            on_press: calculator.calc(entry.text)
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "CE"
            on_press: calculator.del_one(entry.text)
            background_color: .2, .24, .26, 1
        
    BoxLayout:
        spacing: 10
        CustomButton:
            text: "9"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "8"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "7"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "-"
            on_press: app.floating = False
            on_press: 
                if(app.onlyoneparam): entry.text += self.text
                if(app.onlyoneparam): app.onlyoneparam = False
            background_color: .2, .24, .26, 1

    BoxLayout:
        spacing: 10
        CustomButton:
            text: "6"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "5"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "4"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "+"
            on_press: app.floating = False
            on_press: 
                if(app.onlyoneparam): entry.text += self.text
                if(app.onlyoneparam): app.onlyoneparam = False
            background_color: .2, .24, .26, 1

    BoxLayout:
        spacing: 10
        CustomButton:
            text: "3"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "2"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "1"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "/"
            on_press: app.floating = False
            on_press: 
                if(app.onlyoneparam): entry.text += self.text
                if(app.onlyoneparam): app.onlyoneparam = False
            background_color: .2, .24, .26, 1

    BoxLayout:
        spacing: 10
        CustomButton:
            text: "."
            on_press:
                if (app.floating): entry.text += self.text
                if (app.floating): app.floating = False
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "0"
            on_press: app.onlyoneparam = True
            on_press: app.floating = True
            on_press: entry.text += self.text
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "*"
            on_press: app.floating = False
            on_press: 
                if(app.onlyoneparam): entry.text += self.text
                if(app.onlyoneparam): app.onlyoneparam = False
            background_color: .2, .24, .26, 1

        CustomButton:
            text: "%"
            on_press: app.floating = False
            on_press: 
                if(app.onlyoneparam): entry.text += self.text
                if(app.onlyoneparam): app.onlyoneparam = False
            background_color: .2, .24, .26, 1